// Every zsl has its unique ID, IPv6, from hashing some data.
// For example, a car, a human, a concept etc all has its IP address.

function generateZslId() {
    // Hash the return values from the functions above.

    // Register zsl ID to zsl DB.
}